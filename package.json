{
  "name": "lbs-master",
  "version": "0.1.0",
  "private": true,
  "description": "LBS-Master portable sandbox scaffold (Shanghai domain)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/portable-paths.test.js",
    "verify:paths": "node tests/portable-paths.test.js",
    "build": "node -e \"console.log('build: placeholder (WS-B/C will implement)')\""
  }
}
